"""
记忆存储占位实现。

当前阶段该模块只提供类型占位，目的是先稳定包结构与依赖关系，
后续可在此扩展会话摘要、长期记忆检索、向量索引等能力。
"""


class InMemoryConversationStore:
    """
    内存记忆存储占位类型。

    说明：
    1. 当前不承载真实读写逻辑；
    2. 作为未来记忆系统的可替换实现入口；
    3. 通过该类保持上层导入路径稳定，避免后续重构影响业务层。
    """

    # 预留后续扩展方法（如 append/query/summarize）。
    pass
