services:
  agent-service-api:
    build:
      context: .
      dockerfile: Dockerfile
    image: agent-service-api:latest
    container_name: agent-service-api
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: unless-stopped
