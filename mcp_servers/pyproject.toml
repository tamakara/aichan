[project]
name = "mcp-servers"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "cli-mcp-server",
]

[tool.uv.workspace]
members = ["cli"]

[tool.uv.sources]
cli-mcp-server = { workspace = true }
